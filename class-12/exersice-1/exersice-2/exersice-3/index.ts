let post: {
    title: string,
    description: string
}

let title: string = "Hi there!"

let description: string = "Cats are the most lazy pets to have..."

console.log(title);

console.log(description);